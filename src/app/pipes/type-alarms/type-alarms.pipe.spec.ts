import { TypeAlarmsPipe } from './type-alarms.pipe';

describe('TypeAlarmsPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeAlarmsPipe();
    expect(pipe).toBeTruthy();
  });
});
