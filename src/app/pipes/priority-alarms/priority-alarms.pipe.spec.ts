import { PriorityAlarmsPipe } from './priority-alarms.pipe';

describe('PriorityAlarmsPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityAlarmsPipe();
    expect(pipe).toBeTruthy();
  });
});
